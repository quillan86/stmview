function new_map = map_derivative(data,varargin)

% if isstruct(data) % check if data is a full data structure
%     img = data.map;
%     coord = data.r;    
%     [nr nc nz] = size(data);
% else % single data image  
%     img = data;
%     [nr nc nz] = size(data);
%     if isempty{varargin{2})
%         coord = 1:nr; % assume square image for now
%     else
%         coord = varargin{2};
%     end
% end
% new_img = 
% 
% %derivative in x,y,or E (1,2)
% deriv_dir = varargin{1};
% 
% if deriv_dir == 1
%     
% elseif deriv_dir ==2
%    
% else
%     display('Not a valid spatial derivative direction');
%     new_map = [];
%     return;
% end

display('In Progress');
end